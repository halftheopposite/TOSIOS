import dungeon from './dungeon.png';

// This is where you want to reference images needed by your maps
const SpriteSheets: { [key: string]: string } = {
  'dungeon.png': dungeon,
};

export {
  SpriteSheets,
};
